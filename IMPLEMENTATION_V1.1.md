# نسخه 1.1 — پشتیبان کامل، مدیریت تصاویر و جستجوی پیشرفته

## 1) پشتیبان‌گیری و بازیابی
- فرمت جدید `backupVersion=2` داخل ZIP با `backup.json`.
- تصاویر واقعی `media/images/` و صداهای واقعی `media/audio/` داخل پشتیبان.
- ارجاع بوکمارک، پاورقی، نقش من، گفتگو و نوبت گفتگو بر اساس مسیر معنایی زمینه/تعزیه/نقش/بخش، نه ID داخلی Room.
- بازیابی روی دیتابیس تازه، IDهای جدید را پیدا می‌کند و روابط را دوباره می‌سازد.
- پشتیبانی از پشتیبان‌های نسخه 1 برای داده‌های اصلی.
- رمزگذاری AES-GCM برای کل فایل باینری پشتیبان جدید.

## 2) مدیریت تصاویر
- گالری سراسری از حالت فقط‌نمایشی به مدیریت تبدیل شد.
- جستجو در توضیح و نام تعزیه.
- فیلتر بر اساس تعزیه.
- ویرایش توضیح تصویر.
- حذف تصویر از دیتابیس و حافظه داخلی.
- گالری داخل هر تعزیه همچنان برای افزودن تصویر استفاده می‌شود.

## 3) جستجوی پیشرفته
- پنل تنظیمات پیشرفته.
- انتخاب حوزه‌های جستجو: عنوان بخش، متن شعر، نقش، تعزیه، زمینه، واژه/پاورقی.
- محدود کردن تعداد نتایج.
- فیلتر زمینه و تعزیه حفظ شده است.
- جستجوی خودکار هنگام تایپ با تأخیر 250ms برای جلوگیری از اجرای پشت‌سرهم Queryها.

## نکته تست
در محیط ساخت فعلی Gradle Wrapper داخل ZIP وجود ندارد و Android SDK/Gradle قابل اجرای محلی در این محیط موجود نبود؛ بنابراین APK واقعی اینجا ساخته نشده است. کدها از نظر خطای نحوی Kotlin با compiler بررسی شدند و خطاهای گزارش‌شده صرفاً ناشی از نبود وابستگی‌های Android/Compose در classpath مستقل بودند.
